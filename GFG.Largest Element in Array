class Compute {
    
    public int largest(int arr[], int n)
    {
        int select=0;
        int max=0;
        for(int i=0;i<n;i++)
        {
            select=arr[i];
            if(select>max)
            {
                max=select;
            }
        }
        return max;
    }
}
